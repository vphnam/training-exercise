import { SearchByNoFilterPipe } from './search-by-no-filter.pipe';

describe('SearchByNoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByNoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
